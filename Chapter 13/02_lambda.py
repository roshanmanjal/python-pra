def square(n):
    return n * n
print(square(5))  # Output: 25

# this is normal method to define a function


# but we can also use lambda function to do the same thing in a single line
square = lambda n: n*n
print(square(6))  # Output: 36  # this is using lambda function to define the same function